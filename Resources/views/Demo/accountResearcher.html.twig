{% extends 'AcmeFormDemoBundle::layout.html.twig' %}

{% form_theme form 'AcmeFormDemoBundle::div_layout.html.twig' %}

{% block content %}
    <h2>Register Account</h2>

    <form action="{{ path('account') }}" method="POST" novalidate="novalidate">
        
        {{ form_errors(form) }}
        {{ form_widget(form) }}
        {{ form_rest(form) }}
        <input type="submit" value="Save" />
    </form>
{% endblock %}
